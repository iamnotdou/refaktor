{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd .. && pnpm install --no-frozen-lockfile && pnpm build:sdk && cd app && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "echo 'install handled in build step'"
}
