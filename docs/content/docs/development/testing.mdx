---
title: Test Stratejisi
description: Contract, backend, frontend test piramidi
---

# Test Stratejisi

Hackathon'da %100 coverage hedef değil — **kritik akışları** test etmek hedef.

## Smart Contract (Hardhat + Chai)

```typescript
// contracts/test/OrderBook.test.ts
describe("OrderBook", () => {
  it("matches bid >= ask", async () => {
    await invoiceNFT.connect(seller).mintInvoice(...);
    await orderBook.connect(seller).placeAsk(invoiceId, 95, 500);
    
    await usdc.connect(buyer).approve(escrow, 95 * 100);
    await orderBook.connect(buyer).placeBid(invoiceId, 95.5, 100);
    
    // Backend matcher trigger eder, simülasyon
    await orderBook.connect(relayer).executeTrade(...);
    
    expect(await invoiceNFT.balanceOf(buyer, tokenId)).to.equal(100);
  });
  
  it("partial fill works", async () => {
    // Hans 200 wants, only 80 ask available
    // Should fill 80, keep 120 active
  });
  
  it("rejects ask qty > seller balance", async () => {
    await expect(
      orderBook.connect(seller).placeAsk(id, 95, 1000)  // sellr only has 500
    ).to.be.revertedWith("Insufficient balance");
  });
});
```

Coverage hedef:
- InvoiceNFT: %95+
- OrderBook: %85+
- Escrow: %90+
- InsurancePool: %80+

```bash
cd contracts
pnpm hardhat coverage
```

## Backend (Vitest)

```typescript
// backend/src/matching/engine.test.ts
import { MatchingEngine } from './engine';

describe('MatchingEngine', () => {
  it('matches simplest case', async () => {
    const engine = new MatchingEngine(mockContract);
    
    await engine.placeOrder({ type: 'ask', invoiceId: '1', price: 95, qty: 100, maker: 'A' });
    await engine.placeOrder({ type: 'bid', invoiceId: '1', price: 96, qty: 100, maker: 'B' });
    
    expect(mockContract.executeTrade).toHaveBeenCalledWith('1', 'B', 'A', 100, 95);
  });
  
  it('handles partial fill', async () => {
    // ask 80, bid 200 → 80 fill, 120 remaining
  });
  
  it('respects price-time priority', async () => {
    // ilk gelen ask önce match olur (FIFO)
  });
});
```

## Frontend (Vitest + Testing Library)

```typescript
// frontend/src/components/OrderBookTable.test.tsx
import { render, screen } from '@testing-library/react';
import { OrderBookTable } from './OrderBookTable';

it('renders bids and asks', () => {
  const bids = [{ price: 95.5, qty: 100, maker: '0x123' }];
  const asks = [{ price: 95.8, qty: 50, maker: '0x456' }];
  
  render(<OrderBookTable bids={bids} asks={asks} />);
  
  expect(screen.getByText('95.50 €')).toBeInTheDocument();
  expect(screen.getByText('100')).toBeInTheDocument();
});
```

## E2E (Playwright — Phase 2)

Hackathon için **manuel E2E** yeterli. Phase 2'de:
```typescript
test('full flow: upload → list → trade', async ({ page }) => {
  await page.goto('/upload');
  await page.fill('[name=amount]', '50000');
  await page.click('Mint');
  // ...
});
```

## Test Senaryoları (Manuel — Hackathon)

### Akış 1: Happy path
1. KOBİ fatura yükler ✓
2. GİB doğrulama mock ✓
3. Mint başarılı ✓
4. Listele ✓
5. Yatırımcı bid verir ✓
6. Match olur ✓
7. KOBİ USDC alır ✓
8. Yatırımcı token alır ✓

### Akış 2: Partial fill
1. KOBİ 500 token list ✓
2. Hans 200 token bid ver ✓
3. Match 200 ✓
4. Order book'ta 300 ask kalır ✓
5. Hans portföyünde 200 token ✓

### Akış 3: İkincil pazar
1. Hans 200 token sahip ✓
2. Hans 97.5€'dan 100 token ask koyar ✓
3. Erkin 100 token alır ✓
4. Hans realize 250€ kar ✓

### Akış 4: Default
1. Demo invoice'i 30 gün geç bekle (test net'te time travel) ✓
2. Admin declareDefault ✓
3. Sigorta havuzu pay claim ✓
4. Token sahipleri pro-rata ✓
5. Sigorta havuzu bakiyesi azalır ✓

### Akış 5: Edge cases
1. Yetersiz USDC → toast error ✓
2. Yanlış network → switch modal ✓
3. Wallet bağlı değil → connect CTA ✓
4. Backend down → stale data badge ✓

## Hardhat Time Travel

Vade simülasyonu için:
```typescript
import { time } from "@nomicfoundation/hardhat-network-helpers";

await time.increase(120 * 24 * 60 * 60); // 120 gün ileri
await escrow.markPaid(invoiceId, 50000e6);
```

## CI Test Pipeline (Phase 2)

```yaml
test-contracts:
  - cd contracts && pnpm hardhat test
  - cd contracts && pnpm hardhat coverage

test-backend:
  - cd backend && pnpm test

test-frontend:
  - cd frontend && pnpm test

test-e2e:
  - pnpm dev:all &
  - sleep 10
  - pnpm e2e
```

## Hackathon'da Hangileri MUTLAK?

✅ Smart contract happy path testleri (mint, list, trade)  
✅ Backend matching engine birim test  
✅ Manuel demo akışı 3× tekrar  
❌ Frontend snapshot testleri (zaman yok)  
❌ E2E Playwright (zaman yok)  
❌ Coverage report (zaman yok)
